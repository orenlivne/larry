[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "maia-chess"
version = "0.1.0"
description = "Temporary pyproject for Maia integration"
authors = [{ name = "LarryBot", email = "none@example.com" }]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
